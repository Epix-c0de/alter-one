import 'package:flutter/material.dart';

class ParishSelectorScreen extends StatelessWidget {
  const ParishSelectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Parish selector – bind user.parish_id – TODO'),
      ),
    );
  }
}


